## 字符串形式的 Markdown

这个 Markdown 文件将被编译为字符串，而不是 Vue 组件。

<script setup>
// 这些脚本内容会被提取和处理，但不会创建响应式绑定
// 因为这个文件会被编译为纯字符串
const name = 'World'
</script>

例如，这里的变量 `{{name}}` 不会被响应式替换，因为整个文件被作为字符串导出。

### 用例

当你想直接获取渲染后的 Markdown 内容时，这种方式非常有用:

1. 可以将内容复制到剪贴板
2. 可以发送到 API
3. 可以存储到数据库
4. 可以用于静态内容展示 
